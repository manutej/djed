{
  "name": "djed-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Djed - FP-first infrastructure suite for LUXOR projects",
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces",
    "lint": "npm run lint --workspaces --if-present"
  },
  "keywords": [
    "functional-programming",
    "fp-ts",
    "infrastructure",
    "monorepo",
    "luxor"
  ],
  "author": "LUXOR",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.24",
    "typescript": "^5.9.3"
  }
}
