{
  "name": "@djed/mcp-base",
  "version": "0.1.0",
  "description": "Base MCP server class for LUXOR projects",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "bench": "vitest bench --run",
    "bench:watch": "vitest bench",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "server",
    "base-class",
    "luxor"
  ],
  "author": "LUXOR",
  "license": "MIT",
  "dependencies": {
    "@djed/shared-types": "^0.1.0",
    "@djed/logger": "^0.1.0",
    "@djed/validator": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
