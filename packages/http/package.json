{
  "name": "@djed/http",
  "version": "0.1.0",
  "description": "Type-safe HTTP client with TaskEither for async operations and functional patterns",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "clean": "rm -rf dist coverage",
    "prepublishOnly": "npm run clean && npm run build && npm test"
  },
  "keywords": [
    "http",
    "fp-ts",
    "functional-programming",
    "taskeither",
    "circuit-breaker",
    "retry",
    "interceptors",
    "luxor",
    "djed"
  ],
  "author": "LUXOR",
  "license": "MIT",
  "peerDependencies": {
    "fp-ts": "^2.16.11"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "@vitest/coverage-v8": "^1.6.1",
    "fp-ts": "^2.16.11",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}
