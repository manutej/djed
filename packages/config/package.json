{
  "name": "@djed/config",
  "version": "0.1.0",
  "description": "Configuration management with Reader monad for dependency injection",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "clean": "rm -rf dist coverage",
    "prepublishOnly": "npm run clean && npm run build && npm test"
  },
  "keywords": [
    "config",
    "configuration",
    "fp-ts",
    "functional-programming",
    "reader-monad",
    "dependency-injection",
    "environment-variables",
    "schema-validation",
    "luxor",
    "djed"
  ],
  "author": "LUXOR",
  "license": "MIT",
  "peerDependencies": {
    "fp-ts": "^2.16.11"
  },
  "dependencies": {
    "@djed/validation": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "@vitest/coverage-v8": "^1.6.1",
    "fp-ts": "^2.16.11",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}
